header
  nav
    a(href='/overview') Hotel Page
  nav
    img.logotop(src='https://cdn.pixabay.com/photo/2018/05/08/21/28/airbnb-3384008_960_720.png')
  nav
    -console.log(user)
    if user 
        if user.role === 'user'
            .navuserpic
              a.sign.host(href='host') Host Your Home
              a.sign.logout Log out
              a.nav__el(href='/me')
                span= user.username.split(' ')[0]
                img.headerphoto(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        else 
            .navuserpic
              a.sign.logout Log out
              a.nav__el(href='/me')
                span= user.username.split(' ')[0]
                img.headerphoto(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
              
    //- else if user 
    //-     //- a.do_logout Log out
    //-     a.sign.logout Log out
    //-     | 
    //-     //- a.nav__el(href='/me')
    //-     //-     img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
    //-     span= user.username.split(' ')[0]
    
    else
        a.sign(href='/signup') Sign up
        a.sign1(href='/login') Log in
        